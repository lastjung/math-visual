# Incredible Animations: Mathematical Score (Final Complete Version)

**Source:** [Desmos (Math Animation but they get increasingly more INCREDIBLE)](https://www.desmos.com/calculator/be7jzkn4pi)
**Theme:** High-Duration Dynamics and Multi-Variable Interference

---

## 1. Early Dynamic Waves (k1 - k8)
- **Simple Resonators:**
    - `y = sin(k * x)` (Control: `k = 0.5 to 5`, 8s)
    - `y = sin(x) + cos(k2 * x)`
- **Tangent Interferences:**
    - `y = tan(sin(2x) * cos(k3*x)) + sin(k3*x)` (80s Long Cycle)
    - `y = sin(k4*x) * tan(x)`
- **Surface Ripples:**
    - `y = 1.5 * sin(x + cos(k5*x + y))`
    - `y = 5 * sin(x - pi) * cos(k7 * y)`

---

## 2. Global Implicit Fields (k9 - k15)
*Deep mathematical fields using trigonometric nesting.*

- **Logic:**
    - `k9 * sin(cos(y) + sin(x)) = (cos(x) + sin(y))` (Control: `k9 = 5`, 20s)
    - `sin(k10*cos(y) + sin(x)) = (k10*cos(x) + sin(y))`
    - `k11 * sin(cos(y) + sin(k11*x)) = cos(cos(x) + sin(y))` (80s Long Cycle)
    - `sin(k12 * sin(y) * sin(x)) = cos(k12 * cos(x) + sin(y))`
- **Extreme Constraints:**
    - `k15 * sin(sin(x) + cos(y)) = sin(k15*y) - sec(x)`

---

## 3. Circular & Radial Dynamics (k16 - k22)
*Focusing on the distance function x^2 + y^2.*

- **Logic:**
    - `y = x * sin(x^2 + y^2 + k16)` (Spiral ripple)
    - `sin(k17*x) = sin(x^2 + y^2)` (Concentric frequency modulation)
    - `sin(x^2 + y^2) = k18 * cos(xy)` (The "Incredible" Star-grid)
    - `sin(x^2 + y^2 + xy) = sin(k22 + sin(k22*x) + cos(k22*y))` (Extreme Chaos)
- **Control:** `k16 to k22` all use 80,000ms cycles for slow, mesmerizing transitions.

---

## 4. Discrete Transitions
- **GCD Bonus:**
    - `gcd(sin(y), tan(x) + sin(k23 + y)) = 1`

---

## Visual Aesthetics (Classic Symmetry)
| Keyword | RGB Value | Hex (Approx) |
|---|---|---|
| `c_red` | (0, 255, 255) | #00FFFF |
| `c_orange` | (0, 90, 255) | #005AFF |
| `c_yellow` | (0, 0, 255) | #0000FF |
| `c_green` | (255, 0, 255) | #FF00FF |
| `c_cyan` | (255, 0, 0) | #FF0000 |
| `c_blue` | (255, 255, 0) | #FFFF00 |
| `c_purple` | (109, 255, 0) | #6DFF00 |
| `c_pink` | (0, 255, 0) | #00FF00 |

## Global Summary
- **Primary Tool:** Time-dependent coefficients (k1-k23) acting as phase shifters in nested trig functions.
- **Narrative:** "Math Animation but they get increasingly more INCREDIBLE."
- **Total Components:** 23 High-frequency oscillators synchronizing across Cartesian space.
