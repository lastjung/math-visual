# Math Symphony

`math-symphony` is being rebuilt as a score-first system.

Current status:

- `score/` remains the source archive
- `catalog/` holds normalized score data for runtime use
- `engine/` holds controller and expression helpers
- `app/` holds the browser explorer UI

This first pass normalizes:

- `01_amazing_part3`
- `02_more_beautiful`

Open `index.html` in a browser or serve the folder with a static server.
